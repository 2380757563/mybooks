services:

  # main service
  mybooks:
    restart: always
    image: poxenstudio/mybooks
    volumes:
      - /tmp/mybooks:/data
    ports:
      - "3000:80"
      - "3443:443"
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Asia/Shanghai
